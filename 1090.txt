#include <iostream>
using namespace std;
int main() 
{
	int isprime(int a);
    int r,m,n,i,count,sum;
	cin>>r;
	while(r--)
	{
		count=0;sum=0;
		cin>>m>>n;;
		for(i=m;i<=n;i++)
		{
			if (isprime(i))
			{
				count++;
				sum=sum+i;
			}
		}
		cout<<"count="<<count<<",sum="<<sum<<endl;
	}
	  return 0;
}

int isprime(int a)
{
	if (a==1)  return 0;
	for(int i=2;i<a;i++)
	{
		if(a%i==0) return 0;
	}
	return 1;
}