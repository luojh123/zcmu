#include<iostream>
#include<cmath>
#include<iomanip>
using namespace std;  
int main()  
{  
	double a,b;  
	cin>>a;
	if(a>=0)
	{ 
		b=sqrt(a);	
		cout<<"The square root of ";
		cout<<setiosflags(ios::fixed)<<setprecision(1)<<a<<" is "<<b;  
	}
	return 0;
}