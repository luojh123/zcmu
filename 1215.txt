#include <algorithm>
#include <iostream>
using namespace std;
int main()
{
	
	int n,i,a[35];
	int maxi;
	while(cin>>n)
	{
		for(i=0;i<n;i++)
			cin>>a[i];
		maxi=*max_element(a,a+n);
		cout<<maxi<<endl;
	}
	return 0;
}