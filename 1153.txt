#include <iostream>
using namespace std;
int main()
{
    int n;
    while(cin>>n && n)
    {
        if(n%4==0 || (n*3-1)%4==0)
            cout<<"Y\n";
        else
            cout<<"N\n";
    }
    return 0;
}