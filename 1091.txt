#include <stdio.h>
#include <iostream>
using namespace std;
int main()
{
     char s[81];
     int n,i,j,num,word;
     char c;
     cin>>n;
     for (j=1;j<=n+1;j++)
     {
	 num=0;word=0;
	 gets(s);
	 for(i=0;(c=s[i])!='\0';i++)
	     if(c==' ') word=0;
          else if(word==0)
	    {
                word=1;
                num++;
             }
	   if(num!=0)  cout<<num<<endl;
      }
   return 0;
}