#include<cmath>
#include<iostream>
using namespace std;
int isprime(int n)
{
	  if (n == 1)  return 0;
	for(int i=2;i<= (int) sqrt(n);i++)
	{
		if (n%i==0) return 0;
	}
	return 1;
}
int main()
{
	int n; 
	while(cin>>n)
	{
		if(isprime(n)) cout<<"YES"<<endl;
		else cout<<"NO"<<endl;
	}
	return 0;
}